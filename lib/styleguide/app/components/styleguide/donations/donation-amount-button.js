import Component from '@ember/component';

export default Component.extend({
  presetAmount: 5,
  selected: true
  /* BEGIN-FREESTYLE-USAGE donations--donation-amount-button--notes

  ```
  import Ember from 'ember';

  export default Ember.Component.extend({
    presetAmount: 5,
    selected: true
  });
  ```
  END-FREESTYLE-USAGE */
});
