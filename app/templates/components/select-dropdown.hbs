<select onchange={{action "selectItem" value="target.value"}} name={{name}}>
  {{#each items as |item|}}
    {{select-option item=item optionValuePath=optionValuePath optionLabelPath=optionLabelPath selectedItem=selectedItem}}
  {{/each}}
</select>