{{#if isOpen}}
  <button class="button clear" name="close" {{action 'closeTask'}}>Close task</button>
{{else}}
  <button class="button clear" name="open" {{action 'reopenTask'}}>Reopen task</button>
{{/if}}
