{{#if subscription}}
  {{donations/show-donation amount=subscription.amount}}
{{else if processStarted}}
  {{donations/create-donation class="donation-status__create-donation" continue=(action createDonation)}}
{{else}}
  {{donations/become-a-donor class="donation-status__become-a-donor" becomeADonor=(action (mut processStarted) true)}}
{{/if}}
