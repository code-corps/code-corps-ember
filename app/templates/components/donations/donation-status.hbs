{{#if subscription}}
  {{donations/donation-status amount=subscription.amount}}
{{else if processStarted}}
  {{donations/create-donation continue=(action createDonation)}}
{{else}}
  {{donations/donation-status becomeADonor=(action (mut processStarted) true)}}
{{/if}}
