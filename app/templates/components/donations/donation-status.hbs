{{#if subscription}}
  {{donations/donation-status__show-donation amount=subscription.amount}}
{{else if processStarted}}
  {{donations/donation-status__create-donation amount=15 continue=(action createDonation)}}
{{else}}
  {{donations/donation-status__become-a-donor becomeADonor=(action (mut processStarted) true)}}
{{/if}}
