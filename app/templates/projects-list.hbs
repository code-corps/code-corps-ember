{{title "Projects"}}

<div class="container">
  <div class="project-wrapper project-wrapper--index">
    {{#each projects as |project|}}
      {{project-card project=project}}
    {{/each}}
  </div>
</div>
