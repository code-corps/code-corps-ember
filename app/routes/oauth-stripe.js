import Ember from 'ember';

const {
  Route
} = Ember;

export default Route.extend({
  // model(params) {
  //   this.get('store').createRecord('stripe-account', {
  //     code: params.code,
  //     state: params.state
  //   });
  // }
});
